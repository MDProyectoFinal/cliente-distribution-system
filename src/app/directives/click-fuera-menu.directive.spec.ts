import { ClickFueraMenuDirective } from './click-fuera-menu.directive';

describe('ClickFueraMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickFueraMenuDirective();
    expect(directive).toBeTruthy();
  });
});
